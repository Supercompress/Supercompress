# supercompress-cli

Thin CLI alias. Prefer the main package:

```bash
npm install -g supercompress-proxy
supercompress wrap claude
```

See [supercompress-proxy](https://www.npmjs.com/package/supercompress-proxy) for docs, wrap, benchmarks, and setup.
